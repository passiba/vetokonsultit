CREATE TABLE vetokonsultit.luottamustoimi (LUOTTAMUSTOIMIID BIGINT AUTO_INCREMENT NOT NULL, TUNNUS VARCHAR(255), SELITE VARCHAR(255), PRIMARY KEY (LUOTTAMUSTOIMIID))
CREATE TABLE vetokonsultit.jasen (JASENID BIGINT AUTO_INCREMENT NOT NULL, SUKUNIMI VARCHAR(255), PUHELIN VARCHAR(255), TUNNUS VARCHAR(255), SAHKOPOSTI VARCHAR(255), ETUNIMET VARCHAR(255), LUOTTAMUSTOIMIID BIGINT, TYYPPIID BIGINT, PRIMARY KEY (JASENID))
CREATE TABLE vetokonsultit.jasentyyppi (JASENTYYPPIID BIGINT AUTO_INCREMENT NOT NULL, TUNNUS VARCHAR(255), SELITE VARCHAR(255), PRIMARY KEY (JASENTYYPPIID))
ALTER TABLE vetokonsultit.jasen ADD CONSTRAINT FK_jasen_TYYPPIID FOREIGN KEY (TYYPPIID) REFERENCES vetokonsultit.jasentyyppi (JASENTYYPPIID)
ALTER TABLE vetokonsultit.jasen ADD CONSTRAINT FK_jasen_LUOTTAMUSTOIMIID FOREIGN KEY (LUOTTAMUSTOIMIID) REFERENCES vetokonsultit.luottamustoimi (LUOTTAMUSTOIMIID)
